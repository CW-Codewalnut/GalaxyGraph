# @galaxy-graph/adapters

Framework adapters that turn backend source trees and reports into Galaxy Graph's normalized dataset.

Built-ins are registered in `src/registry.ts` so the CLI can discover them without framework-specific branching. See the repository README and `docs/adapters.md` for usage, schema, adapter docs, and release status.
